# OpenShift Multi-Model Qwen3-8B + Gemma IPP Specification (latency blog)
# Real vLLM pods on H100 GPUs for the model-routing benchmark.
# See scenarios/cicd/ocp-qwen-gemma-multi.yaml for details.

# [REQUIRED]
{% set base_dir = base_dir | default('../') -%}
base_dir: {{ base_dir }}

# [REQUIRED]
values_file:
  path: {{ base_dir }}/config/templates/values/defaults.yaml

# [REQUIRED]
template_dir:
  path: {{ base_dir }}/config/templates/jinja

# [OPTIONAL]
scenario_file:
  path: {{ base_dir }}/ipp_benchmarking/config/scenarios/cicd/ocp-qwen-gemma-multi.yaml
